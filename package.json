{
  "name": "pos",
  "version": "1.0.0",
  "description": "A full-featured Point of Sale (POS) application with customer-facing web pages, admin dashboard, API, and mobile staff app.",
  "main": "index.js",
  "scripts": {
    "dev": "concurrently \"npm run dev:api\" \"npm run dev:web\"",
    "dev:api": "cd api && npm run start:dev",
    "dev:web": "cd web && npm run dev",
    "install:all": "npm install && cd api && npm install && cd ../web && npm install",
    "build:all": "cd api && npm run build && cd ../web && npm run build",
    "seed": "cd api && npm run db:seed",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/sundusardian/POS.git"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/sundusardian/POS/issues"
  },
  "homepage": "https://github.com/sundusardian/POS#readme",
  "devDependencies": {
    "concurrently": "^9.2.0"
  }
}
